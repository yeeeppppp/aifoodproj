import { Map } from '../map';
import { SourceAttributes, GeoJsonViewportSourceOptions } from '../types';
/**
 * Class for creating GeoJSON viewport data source in the map.
 */
export declare class GeoJsonViewportSource {
    private options;
    /**
     * Example:
     * ```js
     * const source = new GeoJsonViewportSource(map, {
     *    data: (bbox) => `http://...&outputFormat=application%2Fjson&bbox=${bbox}&srcName=EPSG:4326`,
     * });
     * ```
     * @param map The map instance
     * @param options Initialization options
     */
    constructor(map: Map, options: GeoJsonViewportSourceOptions);
    /**
     * Destroys the source.
     */
    destroy(): void;
    /**
     * Sets the source attributes.
     */
    setAttributes(attributes: SourceAttributes): this;
    /**
     * Gets the source attributes.
     */
    getAttributes(): SourceAttributes;
    /**
     * Sets the source data bbox function.
     *
     * The bbox parameter is automatically calculated based on the visible
     * viewport and the viewportPadding option.
     */
    setData(data: GeoJsonViewportSourceOptions['data']): Promise<void>;
    /**
     * Sets the viewport padding to increase the viewport-based area to get data.
     */
    setViewportPadding(viewportPadding: number): this;
}
