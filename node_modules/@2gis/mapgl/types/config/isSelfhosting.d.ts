/**
 * При сборке проекта вебпак подставляет в process.env.SELFHOSTING
 * конкретное значение, которое содержится в переменной isSelfhosting.
 *
 * С ее помощью мы можем устанавливать для селфхостинга свои, более удобные
 * дефолты и шаблонизировать пути до бекендов.
 */
export declare const isSelfhosting: boolean;
